[package]
name = "test-fixture"
version = "0.1.0"
edition = "2021"

[workspace]
members = ["glob-source", "glob-inner", "named-source"]

[dependencies]
glob-source = { path = "glob-source" }
named-source = { path = "named-source" }
